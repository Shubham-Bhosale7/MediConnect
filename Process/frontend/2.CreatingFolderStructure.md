1. Components
  1. <Link> tag has to property which is used to navigate to different page
  2. onClick={() => {
              navigate(`/appointment/${item._id}`);
              scrollTo(0, 0);
              }}

2. Pages - To add multiple pages we need react router dom in main.jsx
  1. Main.jsx
      <BrowserRouter>
        <App />
      </BrowserRouter>,

  2. App.jsx
     const App = () => {
     return (
       <div className="mx-4 sm:mx-[10%]">
       <Routes>
         <Route path="/" element={<Home />}> 
         </Route>
       </Routes>
       </div>
     )
   }

  3. 

3. Context