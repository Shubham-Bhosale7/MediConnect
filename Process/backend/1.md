1.  create server.js
2.  Run Commands
	 1. npm init
	 2. npm install express mongoose multer bcrypt cloudinary cors dotenv jsonwebtoken nodemon validator
3.  Then we import express in server.js
	 1. import express from "express";
	 2. const app = express();
		 const port = process.env.PORT || 4000;

	 3. app.get("/", (req, res) => {
		 res.send("API WORKING Great hello is");
		 });
	 4. app.listen(port, () => console.log("Server Started", port));

4.  Connecting mongodb
  1. const DB = process.env.MONGODB_URL.replace(
		  "<db_password>",
		  process.env.DATABASE_PASSWORD,
		  );

  2. const connectDB = async () => {
			mongoose.connection.on("connected", () => console.log("Database Connected"));

			await mongoose.connect(DB, {
				dbName: "mediconnect", // 👈 This explicitly targets your database safely
			});
};

5. Connecting Cloudinary
  1. import { v2 as cloudinary } from "cloudinary";

	2. const connectCloudinary = async () => {
  cloudinary.config({
    cloud_name: process.env.CLOUD_NAME,
    api_key: process.env.CLOUDINARY_API_KEY,
    api_secret: process.env.CLOUDINARY_SECRET_KEY,
  });
};
